# Context Notes

작업 중 내린 결정과 그 이유를 계속 덧붙인다. 다음 세션이 다시 추론하지 않도록.

---

## 2026-09-08 — 미결 항목 정리

### 조사에서 확인한 사실

- `reply_to_id` 체인은 이미 검증됐다. `data/history.jsonl`에 9/7 `success` 2건이 있고 둘 다 `replyIds` 4개가 채워져 있다. ADR-009가 "실 테스트로 확인 예정"으로 남아 있던 건 문서가 코드 현실을 못 따라간 것뿐이다.
- `link_attachment`는 **한 번도 실행된 적이 없는 코드 경로다.** `content/queue/*.json` 13개 중 `link` 필드를 가진 파일이 하나도 없다. 즉 TDD 13-2는 "확인 안 됨"이 아니라 "확인할 기회조차 없었음"이다.
- 유일한 `partial` 사고는 9/7 05:47 한 건이고 원인은 error 100 "does not exist"였다. 이건 컨테이너 전파 지연이었고 이미 재시도 대상으로 고쳐졌다(ADR-014로 기록).

### 결정 1 — 알림 채널을 못 박지 않고 범용 웹훅으로 간다 (ADR-012)

카카오톡 나에게 보내기는 OAuth 토큰을 또 저장·갱신해야 해서 `core/token.js`급 관리 코드가 하나 더 생긴다. SMTP는 `nodemailer` 의존성이 붙어 "의존성 3개" 원칙이 깨진다. 둘 다 채널 하나에 코드를 묶는다.

대신 `NOTIFY_WEBHOOK_URL` 하나만 두고 axios로 POST 1회를 보낸다. axios는 이미 있으므로 신규 의존성 0이고, Slack·Discord·카카오워크 어디로든 URL만 바꾸면 된다. 채널 결정을 코드가 아니라 `.env`로 미룬 셈이다.

페이로드는 `{ text, content }` 두 키를 같이 보낸다. Slack은 `text`를, Discord는 `content`를 읽고 서로 모르는 키는 무시한다. 어댑터 레이어를 만들 만한 규모가 아니라서 키 두 개로 끝냈다.

### 결정 2 — 알림 호출부는 2곳뿐

- `index.js`의 발행 실패 catch — `partial`도 예외로 올라오므로 이 한 곳이 failed와 partial을 모두 덮는다.
- `core/token.js`의 갱신 실패 + 잔여 2일 미만 — 무인 운영에서 조용히 죽는 유일한 경로라서 넣었다.

성공 알림은 넣지 않았다. 매일 오는 성공 알림은 곧 무시하게 되고, 성공은 `history.jsonl`에 남는다.

알림 실패는 `logger.warn`으로 삼킨다. 알림이 안 갔다고 발행이 실패하면 주객이 전도된다.

전송 전에 `logger.mask()`를 태운다. 오류 메시지에 URL 쿼리스트링이 섞여 들어올 수 있어서 토큰이 외부 웹훅으로 새는 걸 막는다.

### 결정 3 — 자동 이어쓰기(TDD 13-1)는 보류

원래 근거였던 `partial` 사고의 원인이 ADR-014로 사라졌다. 재발 빈도가 낮은 상태에서 이력 구조를 복잡하게 만들 이유가 없다. 다시 `partial`이 2회 이상 쌓이면 착수한다는 조건을 TDD에 적어 뒀다.

### 결정 4 — `link_attachment` 실 테스트는 사람이 한다

확인하려면 `@flag_21`에 실제로 글이 올라가야 한다. 되돌릴 수 없는 외부 작업이라 자동으로 실행하지 않았다. README에 절차(큐 파일에 `link` 추가 → `--dry-run` → `--now` → 눈으로 확인)만 남겼다.

---

## 2026-09-10 — 전체 시스템 구조·파이프라인 점검 (/plan-eng-review)

전체 코드베이스 4섹션 리뷰. 발견 8건 전부 사용자 결정 후 반영했다.

### 구조 자체는 손대지 않았다

계층 분리(client/publisher/token/history/source)는 파일 15개 규모에 과하지 않고, 소스 플러그인 인터페이스도 2·3차 확장 계획이 있어 정당하다. 발견된 문제는 **전부 "무인 운영에서 조용히 어긋나는 자리"**에 몰려 있었고, 성능·복잡도 쪽엔 지적할 게 없었다.

### 결정 1 — 하루 상한을 상태 이름이 아니라 mainId로 센다 (ADR-015)

9/7에 실제로 상한이 깨졌다(05:47 partial → 06:06 success, 상한 1인 날 글 2개). `partial`은 메인이 이미 올라간 상태인데 `countToday`가 `success`만 셌기 때문이다. 상태 목록에 `partial`을 더하는 대신 `mainId` 유무로 바꿨다 — 상태를 새로 만들 때 목록 갱신을 기억할 필요가 없어진다. 기존 `countToday` 테스트 픽스처에 `mainId`를 채워 넣었다(실제 이력 모양과 같아진 것이라 픽스처가 더 정확해졌다).

### 결정 2 — 알림에 주 1회 생존 신호를 추가 (ADR-012 개정)

9/8에 붙인 알림 훅에 구조적 빈틈이 있었다. 실패 알림은 `run()`이 돌아야 울리는데, 스케줄러가 죽으면 발행 시도 자체가 없어 실패도 없고 알림도 영원히 안 온다. 즉 **가장 위험한 실패 모드를 알림이 못 잡고 있었다.** 하트비트 파일 + 외부 감시 방식도 후보였으나 "감시자를 누가 감시하나" 문제가 생겨, 1인 PC 구성에선 주 1회 신호가 맞다. 침묵이 곧 신호가 된다.

### 결정 3 — 락에 나이 상한 (ADR-016)

`isAlive`가 `EPERM`을 "살아 있음"으로 처리하는데, OS가 pid를 재사용하면 영원히 "실행 중"으로 판정돼 매일 조용히 건너뛴다. 증상이 "아무 일도 안 일어남"이라 원인 찾기가 제일 오래 걸리는 종류다. 락에 이미 있던 `startedAt`을 쓰므로 비교문 한 줄로 끝났다.

### 결정 4 — validator가 replies 계약을 확정 (P2)

validator는 `replies: undefined`를 통과시키는데 publisher는 `item.replies.length`를 바로 읽는다. 지금 안 터지는 건 queueSource가 항상 배열로 채워주기 때문이지 설계가 맞아서가 아니다. 계획된 `llmSource`가 이 계약을 깨면 **메인 글이 올라간 뒤** TypeError로 죽는다. 호출부마다 `|| []`를 까는 대신 검증 단계에서 확정지었다.

### 결정 5 — front-matter가 본문을 덮지 못하게 (P3)

`{ text, replies, ...meta }`에서 front-matter에 `text:`를 적으면 본문이 통째로 덮였다. 스프레드 순서만 뒤집는 대신 `link`·`publishAt`만 명시적으로 뽑았다 — 지원 키가 코드에 드러나 문서 역할을 한다.

### 결정 6 — getPublishingLimit 삭제 (P2)

정의만 있고 호출 0곳이었다. 하루 1건 발행에 서버 쿼터를 물을 이유가 없고 로컬 상한이 이미 더 빡빡하다. 필요해지면 git 이력에서 꺼내 쓴다.

### 결정 7 — content/published/를 git 추적 (P2)

발행할 때마다 `D content/queue/...`가 남던 문제. published를 추적하면 삭제가 아니라 이동으로 보인다. **`content/failed/`는 여전히 ignore라 실패 시엔 같은 삭제 diff가 남는다** — 실패가 드물어 일단 두었고, 거슬리면 같은 방식으로 풀면 된다.

### 결정 8 — 테스트를 publisher 조립 라인까지 (39개 → 55개)

지금까지 실제로 터진 사고는 전부 테스트가 0개인 영역(publisher, run)에서 나왔다. `publishThread`가 내부에서 `ThreadsClient`를 직접 만들어 테스트가 불가능했는데, `{ client }` 주입 자리를 하나 열어 가짜 클라이언트로 **댓글 사슬 순서(각 댓글의 `reply_to_id`가 직전 게시물 id)**와 partial 경로를 처음으로 고정했다. 이 순서가 조용히 깨지면 사람이 Threads 앱을 눈으로 봐야만 알아챌 수 있어서, 기계 검사 가치가 가장 큰 지점이다.

테스트 작성 중 첫 실행이 3개 실패했는데 원인은 `isDuplicate`였다 — 헬퍼가 매번 같은 본문을 써서 2번째 테스트부터 중복으로 걸러졌다. 테스트마다 다른 본문을 쓰도록 고쳤다. 프로덕션 코드의 중복 방지가 제대로 동작한다는 방증이기도 하다.

### 안 한 것

- **자동 이어쓰기**는 여전히 보류(ADR-014로 원인 해소). 단 ADR-015로 partial이 상한을 먹게 되어, partial 당일 수동 복구는 `MAX_POSTS_PER_DAY`를 일시적으로 올려야 한다. 트레이드오프를 알고 받았다.
- **outside voice(codex 교차 검토)**는 돌리지 않았다. 이 환경엔 codex CLI가 없고, 서브에이전트는 사용자 요청 없이 띄우지 않는다는 규칙이 있다.
