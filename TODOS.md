# TODOS

2026-09-10 전체 시스템 점검(`/plan-eng-review`)에서 **의도적으로 미룬** 항목들이다.
발견 8건은 그날 전부 반영했고(커밋 `89fac1c`~`4994b7b`), 아래는 그때 범위 밖으로 둔 것들이다.

각 항목은 3개월 뒤에 읽어도 왜 미뤘는지 알 수 있게 배경까지 적는다.
맥락 없는 TODO는 없느니만 못하다 — 아이디어를 잡아뒀다는 착각만 남기고 근거는 잃기 때문이다.

---

## 1. `content/failed/`도 git 추적하기

**무엇** `.gitignore`에서 `content/failed/`를 빼서, 발행 실패 시에도 파일이 사라지는 게 아니라 옮겨가는 것으로 보이게 한다.

**왜** 9/10에 `content/published/`는 추적으로 돌렸는데(커밋 `2667f7b`) `failed/`는 그대로 뒀다. 그래서 **발행이 실패하면 여전히 `D content/queue/...` 삭제 diff가 남고** 사람이 그걸 따로 커밋해야 한다. 무인 시스템인데 사람 손이 필요해지는 구조가 절반만 고쳐진 상태다.

**장점** 실패한 콘텐츠도 저장소에 남아 나중에 고쳐 다시 쓸 수 있다. git status가 완전히 조용해진다.

**단점** 없다시피 하다. `.gitignore` 한 줄이다. 굳이 꼽자면 실패 파일이 저장소에 쌓인다.

**맥락** 리뷰 당시 사용자 결정이 "`published/`도 추적"이었고, 결정 문구에 없던 `failed/`까지 손대는 건 범위 확대라 남겨뒀다. 지금까지 `failed/`로 간 파일은 0건이라 실물 피해는 없다.

**선행 조건** 없음. `.gitignore`에서 `content/failed/` 한 줄 삭제 + `git add content/failed/`.

---

## 2. `index.js` `run()` 통합 테스트

**무엇** 락 획득/stale 판정, 하루 상한 차단, `onPublished`/`onFailed` 분기를 실제로 `run()`을 돌려 검증하는 테스트.

**왜** 지금까지 실제로 터진 사고는 **전부 테스트가 없던 영역**에서 나왔다(9/7 partial 사고, 하루 상한 뚫림). `publisher`는 9/10에 덮었지만 `run()`은 여전히 0개다. 순수 함수(`isStaleLock`, `countToday`)는 덮었으므로 남은 건 그것들을 **엮는 배선**이다.

**장점** 커버리지가 90%를 넘고, 특히 락 동작(ADR-016)이 기계 검사로 고정된다.

**단점** `run()`은 `fs`, `process.kill`, 토큰, 소스를 전부 건드려서 모킹 분량이 테스트 본체보다 커질 수 있다. 리뷰에서 이 이유로 8A(publisher까지)를 골랐다.

**맥락** 리뷰 이슈 8의 옵션 C였고 사용자가 A를 선택했다. 재검토 신호는 "락이나 상한 쪽에서 사고가 한 번 더 나면".

**선행 조건** 없음. 다만 `publishThread`에 열어둔 `{ client }` 주입 자리처럼, `run()`에도 소스/토큰 주입 자리가 필요할 수 있다.

---

## 3. `isDuplicate`가 `partial`을 중복으로 치지 않는 점

**무엇** `core/history.js`의 `isDuplicate`는 `status === 'success'`인 이력만 본다. `partial`로 끝난 글은 중복 판정을 받지 않는다.

**왜** 9/10에 `countToday`는 `mainId` 기준으로 바꿨는데(ADR-015) `isDuplicate`는 그대로 뒀다. **두 함수가 서로 다른 기준으로 같은 이력을 읽는 상태다.** 의도된 것으로 보이기는 한다 — partial 난 글을 고쳐서 다시 올릴 수 있어야 하니까. 하지만 그 의도가 코드에도 ADR에도 적혀 있지 않다.

**장점** 기준을 통일하거나, 통일하지 않는 이유를 명시하면 다음 사람이 헷갈리지 않는다.

**단점** 성급하게 `partial`도 중복으로 치면, partial 복구를 위해 같은 파일을 다시 큐에 넣는 정상 절차가 막힌다. **고치기 전에 의도부터 확정해야 한다.**

**맥락** 리뷰에서 발견했으나 사용자 결정 항목으로 올리지 않았다(변경이 아니라 의도 확인이 필요한 사안이라). 결론이 "지금이 맞다"여도 ADR-015에 한 줄 덧붙이면 끝난다.

**선행 조건** ADR-015를 읽고 의도를 먼저 정할 것.

---

## 4. `link_attachment` 미리보기 카드 실검증

**무엇** 큐 파일에 `link`를 넣고 실제로 1건 발행해, 미리보기 카드가 뜨는지와 본문 안 URL과 중복 표기되는지 눈으로 확인한다.

**왜** **이 코드 경로는 아직 한 번도 실행된 적이 없다.** 큐 파일 어디에도 `link` 필드가 없어서다. 구현은 돼 있고(`createTextContainer`의 `linkAttachment`) 검증만 안 됐다.

**장점** TDD 13-2가 닫힌다. 블로그 유입을 노리는 글에서 링크를 쓸 수 있는지가 확정된다.

**단점** 실계정에 실제로 글이 올라간다. 결과가 나쁘면(중복 표기 등) 수동 삭제해야 한다.

**맥락** 되돌릴 수 없는 외부 작업이라 에이전트가 임의로 실행하지 않았다. 절차는 README "링크 첨부 확인"에 있다 — `link` 추가 → `--dry-run`으로 로그 확인 → `--now` → Threads 앱에서 확인 → TDD 13-2에 결과 기록.

**선행 조건** 사람이 직접 해야 한다. 하루 상한에 걸리므로 그날 다른 발행이 없을 때.

---

## 5. 댓글 자동 이어쓰기 (보류 중)

**무엇** `partial` 이력의 `publishedReplies` 개수만큼 건너뛰고 나머지 댓글부터 재개하는 기능.

**왜 보류인가** 착수 근거였던 9/7 partial 사고의 원인이 컨테이너 전파 지연이었고, 이미 재시도 대상으로 처리해 사라졌다(ADR-014). **사고 자체를 없앴으므로 사고 후 복구 장치의 우선순위가 내려갔다.**

**장점** partial 복구가 자동화된다.

**단점** 이력 구조가 복잡해진다. 발생 빈도가 낮은 상태에서 지불할 값이 아니다.

**맥락** TDD 13-1에 재착수 조건을 적어뒀다 — **`partial`이 2건 이상 더 쌓이면 구현한다.** 현재 누적 1건(2026-09-07).

**같이 볼 것** ADR-015로 `partial`도 하루 상한을 먹게 되어, **partial 난 당일에 수동 복구하려면 `MAX_POSTS_PER_DAY`를 잠깐 올려야 한다.** 이 불편이 반복되면 자동 이어쓰기의 근거가 다시 살아난다.

**선행 조건** 없음. 다만 착수 전에 `isDuplicate` 기준(위 3번)을 먼저 정해야 한다 — 재개 로직이 중복 판정과 맞물린다.
